{
  "name": "miraverseosx-root",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,magenta\" \"dotnet run --project MiraverseOSx.csproj\" \"npm run dev --prefix frontend\"",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,magenta\" \"dotnet run --project MiraverseOSx.csproj\" \"npm run dev --prefix frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "dotnet run --project MiraverseOSx.csproj",
    "build": "npm run build --prefix frontend && dotnet build",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "dotnet build",
    "preview": "npm run preview --prefix frontend",
    "orchestrator": "npm run dev --prefix multi-role-terminal-orchestrator",
    "data:build": "python scripts/build_data.py"
  },
  "description": "MIRAVERSEOSX - Celestial Operating System Desktop Host",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mirxclej/MiraverseOSx.git"
  },
  "license": "ISC",
  "dependencies": {
    "@tanstack/react-query": "^5.101.4",
    "bootstrap": "^5.3.8"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
